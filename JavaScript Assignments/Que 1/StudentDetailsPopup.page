<apex:page standardController="Student__c" showHeader="false">
    <apex:pageBlock title="{!Student__c.First_Name__c}">
    	<apex:pageBlockTable value="{!Student__c}" var="student">
        	<apex:column value="{!student.Name}" />
            <apex:column value="{!student.Class__c}" />
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>